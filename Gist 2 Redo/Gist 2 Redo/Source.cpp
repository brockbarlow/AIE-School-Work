/*Gist 2 Description:
"Write a c++ program that creates the identity matrix. When they are done, run it from
the desktop and output it."*/

#include "Header.h"

//void createIdentity(int, int);

//int rows;
//int cols;

int main()
{
	//std::cout << "Enter # of rows: " << std::endl;
	//std::cin >> rows;
	//std::cout << std::endl;

	//std::cout << "Enter # of columns: " << std::endl;
	//std::cin >> cols;
	//std::cout << std::endl;

	//createIdentity(rows, cols);

	system("pause");
	return 0;
}